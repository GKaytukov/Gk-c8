const legalAge = 21;
const isDrinkingAge = 21; 
    

function isDrinkingAge (legalAge){
    
    if(age >=21  || isDrinkingAge) {
        console.log('You can drink!')
    } else if (age >= 18){
        console.log('You can not drink!')
    }else {
        console.log('You can stay home.')
    }
}
isDrinkingAge(35);

// I am going to check the age if is over 21 then they can drink and if not access denied 

console.log(isDrinkingAge); 

//Define our function: 
function isDrinkingAge(age){
    const drinkingAge = 21; 
    const canDrink = age
}




function computeAreaOfTriangle()
